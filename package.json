{
  "name": "stack-visualizer",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm install --prefix string-pattern-visualizer && npm run build --prefix string-pattern-visualizer && npm install --prefix tree-visualizer && npm run build --prefix tree-visualizer && node copy-dist.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsdom": "^29.1.1",
    "node-fetch": "^2.7.0"
  }
}
